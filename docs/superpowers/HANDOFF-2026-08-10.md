# Handoff — 2026-08-10

Written at the end of a long session so the next one can start cold. Everything below is
committed; the working tree is clean.

## Where the code is

**Branch: `feat/item-status-typing`** — this is the tip, and it carries three waves of work.
It is **unmerged**, and it forks from `docs/registration-flow-design`, which is itself
unmerged from `main`. Do not rebase onto `main` — main lacks the ApexFlow Plan 3 merge that
all of this depends on.

```
main
 └── docs/registration-flow-design      (unmerged)
      └── feat/item-status-typing       (unmerged) ← YOU ARE HERE, 51889d2
```

### What landed, in order

1. **Typed item-status vocabulary** — `ItemStatus` StrEnum as the single source; `in_progress`
   dropped everywhere; `WorkflowItem` pydantic row model; narrowed wire payload; TypeScript
   vocabulary generated from Python with a drift test.
2. **Workflow form section titles + descriptions** (merge `8c05d2a`, 25 commits) — sections
   gain `title`/`description`; `fieldset`/`legend` in every layout; completion-aware accordion
   for parents; section rail for staff ≥700px; markdown descriptions via a single hardened
   `SectionDescription` wrapper; designer authoring inputs; enrollment template backfilled.
3. **Two designer cold-start fixes** (`6a8375d`, `cadd3b8`) — new workflows seed a valid
   `draft --submit--> done` machine instead of an empty one; adding a stage now fills the
   missing role instead of always being `active`.

### Verified state at handoff

workflow-forms **59** · apexflow **553** · familyhub **89** · admindash **201** · datacore **354**
· admindash vitest **94**. All three frontends build clean. familyhub bundle 114.50 kB gzip.

`admindash npm run lint` reports **5 pre-existing errors** (DynamicForm, AuthContext,
DashboardContext, ModelContext) that are present on the base branch and unrelated to this
work. Do not fix them; do notice if a 6th appears.

## What's next

**Write the implementation plan for the stage-centric workflow editor.**
Spec: `docs/superpowers/specs/2026-08-10-stage-centric-workflow-editor-design.md` (approved).

The spec's own risk section says the first task should be **writing a second workflow
template (signup)** — not editor code. Enrollment is currently the only template that
exists, so the stage abstraction has been validated against exactly one example. A second
template is needed regardless, costs far less than the editor, and is the only way to learn
whether the abstraction holds before the Machine tab is deleted.

### Decisions already made (do not relitigate)

| Decision | Ruling |
|---|---|
| Design center | An admin **adapting a template**, not building from nothing |
| Cross-cutting exits | Separate **Exits panel** — one rule, expanded to per-stage transitions on save |
| Guards/effects | **Plain language with an escape hatch** to the existing composer |
| Machine tab | **Replaced outright** |
| From scratch | Supported but **not optimized** — never a blank canvas |
| Crowd-sourced templates | **Deferred.** Blocker is model portability, not authoring |

## Open items

### Follow-ups logged in `docs/superpowers/plans/2026-08-05-apexflow-plan1-followups.md`

- **25** — `message`-step bodies are still plain text while section descriptions render markdown.
- **26** — **no CI runs any test suite.** `.github/workflows/` has only `deploy.yml` and
  `discord-release.yml`. Every suite is run locally by whoever remembers. This now matters
  more: workflow-forms's vitest suite holds the markdown containment and link-scheme tests,
  which are a security boundary.
- **27** — the form-section spec and plan describe `disableParsingRawHTML` behavior that
  `markdown-to-jsx@9.10.2` does not have. The shipped component adds a `stripRawHtml`
  pre-filter neither document mentions. Security is unaffected; the docs are wrong.
- **28** — **a regression this work introduced.** `_MD_LINK_REF_R` in `validate.py`
  over-rejects ordinary prose: `[Important]: read the handbook` is blocked at publish with an
  error naming a link that does not exist, while the renderer displays it fine. Fails closed,
  admin-facing, rephrase works around it. One-line fix identified in the entry.

### Known-incomplete, recorded rather than claimed done

- **The original section-ambiguity bug is half fixed.** A parent can now distinguish the
  student's "First name" from an emergency contact's. But all repeat rows within
  `contacts_section` share one `fieldset`, so contact #1 and contact #2 are still announced
  identically, and all five "Remove" buttons share one accessible name.
- **familyhub's resume-ordering fix is not unit-guarded** — familyhub and admindash frontends
  have no test framework, so it rests on code review plus build/lint.
- **Flattened lists lose bullets and numbers** by design (approved decision). `1. one / 2. two`
  renders as two unnumbered lines.

### Still unspecced

- **The AdminDash home page.** Two specs defer to "a separate spec" for it; that spec does not
  exist. "Needs you today" and "Enrollment pipeline" both read the legacy `lead` entity
  (`HomePage.tsx:99` — *"Leads drive both the pipeline spine and the work queue"*) and know
  nothing about ApexFlow workflow instances. The open product question before any design:
  enrollment now lives in two systems — does the home page show both, replace leads with
  workflow instances, or merge them?

## Local environment

All 11 services were started via `./start-services.sh` and were healthy at handoff; logs in
`.logs/` were clean. Restart with the same script (default mode kills existing and restarts
all). Ports are in `services.json`.
