# Tasks: Dark Theme Redesign

## 1. Core: Global CSS variables

- [x] 1.1 Replace `:root` color palette in `index.css` with dark theme values
- [x] 1.2 Update `body` and noise texture opacity for dark background
- [x] 1.3 Update scrollbar colors for dark theme

## 2. Core: App shell & shared styles

- [x] 2.1 Update `App.css` — header (dark bg, gradient logo), `.card` (glassmorphism), `.btn--primary` (gradient + hover lift)
- [x] 2.2 Update `App.css` — `.input`, `.select`, `.badge`, `.spinner` for dark backgrounds
- [x] 2.3 Update `App.css` — shadow variables and overlay backdrop

## 3. Components

- [x] 3.1 Update `EntityCard.css` — dark table rows, toggle track, options editor tags
- [x] 3.2 Update `FileUploader.css` — dark drop zone, purple corner accents
- [x] 3.3 Update `TenantInfo.css` — colors for dark bg
- [x] 3.4 Update `ModelSelector.css` — dark select styling

## 4. Pages

- [x] 4.1 Update `LandingPage.css` — dark model card, action cards with glass treatment
- [x] 4.2 Update `UploadPage.css` — dark form area, overlay backdrop, progress bar with purple gradient
- [x] 4.3 Update `ReviewPage.css` — dark stats bar, source panel, no-changes notice
- [x] 4.4 Update `FinalizedPage.css` — dark meta bar, entity summary tables, JSON preview, confirm banner

## 5. Verify

- [x] 5.1 Check login → landing page transition is visually seamless
- [x] 5.2 Verify all pages render correctly with dark theme
