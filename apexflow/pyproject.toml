[project]
name = "apexflow-backend"
version = "0.1.0"
description = "Generic workflow engine backend, generalized from enrollx-backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "httpx>=0.28",
    # Chat workflow builder (Plan 4). Same CONSTRAINT admindash pins for its
    # own chat assistant — but not the same resolved version: the two services
    # have independent lockfiles resolved on different dates (admindash pins
    # 2.18.0, this lock resolved 2.30.0). Sharing the floor keeps the two
    # chat implementations portable between services; it does not keep them
    # on one version, so an import that works in admindash still has to be
    # verified here.
    "pydantic-ai>=0.0.30",
]

[project.optional-dependencies]
# httpx2 is required (not just recommended) by Starlette 1.3.1's testclient
# module: it imports httpx2 and, when only plain httpx is present, emits
# StarletteDeprecationWarning on every TestClient use ("Using `httpx` with
# `starlette.testclient` is deprecated; install `httpx2` instead" —
# starlette/testclient.py:33,36,48). Ported from enrollx/pyproject.toml,
# which carries the same comment for the same reason.
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "respx>=0.21",
    "httpx>=0.28",
    "httpx2>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["backend/app"]
